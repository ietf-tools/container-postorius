{{- if .Values.mailmanSecret.create -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.mailmanSecret.name }}
  labels:
    {{- include "postorius.labels" . | nindent 4 }}

type: Opaque
data:
  djangoSecretKey: {{ .Values.mailmanSecret.data.djangoSecretKey | b64enc }}
  mailmanApiUser: {{ .Values.mailmanSecret.data.mailmanApiUser | b64enc }}
  mailmanApiPass: {{ .Values.mailmanSecret.mailmanApiPass | b64enc }}
{{- end }}
