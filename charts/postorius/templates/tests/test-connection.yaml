apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postorius.fullname" . }}-test-connection"
  labels:
    {{- include "postorius.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postorius.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
